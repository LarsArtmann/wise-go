# wise-go v0.10.0

> Draft release notes — paste into `gh release create v0.10.0` (or the GitHub
> Releases UI) once the annotated tag is approved and pushed (TODO_LIST P1).

The proof-of-payment release: everything needed to evidence a completed
transfer after the money has moved. 33 client methods total, up from 31 in
v0.9.0.

## Highlights

- **`GetTransferReceipt(transferID)`** — download the branded confirmation
  receipt for a paid-out transfer as raw PDF bytes
  (`GET /v1/transfers/{transferId}/receipt.pdf`). Wise answers 404 until the
  transfer reached `outgoing_payment_sent`, so `*NotFoundError` means "no
  receipt (yet)", not a hard failure. Not SCA-protected; available to personal
  API tokens in all regions.
- **`GetTransferPayoutInfo(transferID)`** — banking-partner payout information
  (`GET /v1/transfers/{transferId}/invoices/bankingpartner`) as
  `*TransferPayoutInfo`: processor, delivery mode, the partner bank's tracking
  reference, and the SWIFT MT103 message (`*string`, nil for non-SWIFT
  corridors). Accounting-grade proof-of-payment retrieval.

## Changed

- Go toolchain requirement bumped to **1.26.7** (`go.mod` `go` directive,
  commit `14523ae`); nix flake inputs pinned and the CI workflow file refreshed
  (the workflow itself remains disabled on GitHub).

## Compatibility

Additive only — no existing signatures, types, or behaviors changed. Bump the
Go toolchain if you are below 1.26.7; nothing else to do.
