# wise-go v1.0 API Audit — Findings Report

> **Re-audit (2026-09-13, full-execution pass — supersedes the note below):** the surface
> is now **33 `*Client` methods / 92 exported types / 22 const groups** (`go doc -all`,
> go1.26.7). Lineage from this audit's snapshot: v0.9.0 added
> `RefreshQuoteAccountRequirements` (tagged `e508572` the same evening; this audit's
> list named 30 of its claimed 31 methods — the missing entry was that one), and
> v0.10.0 added `GetTransferReceipt` + `GetTransferPayoutInfo` (`fe896a8`). Re-audit
> godoc pass: all 3 new methods' docs are spec-accurate; one stale claim found and
> fixed (`ListBalances` still said Wise exposes no per-balance endpoint — false since
> v0.9.0's direct-endpoint `GetBalance`). Risk register extended for the new methods
> (§3 items 8–10). The surface remains v1.0-coherent; the v1.0.0 tag stays gated on
> user approval.
>
> **Delta after the re-audit (same-day webhook pass, 2026-09-13):** +4 `*Client`
> methods (profile webhook-subscription CRUD on the `/2026Q3` surface) / +15
> exported types / +3 const groups → **37 methods / 107 types / 25 const groups
> across 15 resources**; `ParseWebhookEvent` is a package-level function, not a
> `*Client` method.

**Date:** 2026-08-21 14:00 CEST
**Scope:** every exported symbol of `package wise` (single public package + `internal/raw` invisible to consumers), godoc accuracy, and the breaking-change risk register required before tagging v1.0.
**Baseline:** 31 `*Client` methods, 74 exported types, 70 exported functions/methods/const groups. 121 BDD specs + ~90 unit tests, 0 lint issues.

---

## 1. Exported-symbol inventory (the v1.0 surface)

### Client methods (31 at audit time — 33 as of the 2026-09-13 re-audit, 37 after the same-day webhook pass)

Identity: `GetMe`, `GetUser`, `GetProfile`, `ListProfiles`, `Authenticate`, `Health`
Balances/MCA: `ListBalances`, `GetBalance`, `CreateBalance`, `GetTotalFunds`, `GetMultiCurrencyAccount`, `GetBankAccountDetails`, `ListCurrencies`
Transactions/statements: `ListTransactions`, `GetStatement`
Quotes: `CreateQuote`, `CreateUnauthenticatedQuote`, `GetQuote`, `GetQuoteAccountRequirements`
Recipients: `ListRecipients`, `GetRecipient`, `CreateRecipient`
Transfers: `ListTransfers`, `GetTransfer`, `CreateTransfer`, `CancelTransfer`, `FundTransfer`, `GetDeliveryEstimate`, `ValidateTransferRequirements`
Rates: `GetExchangeRate`

**Growth since this list (2026-09-13 re-audit, `go doc -all` on go1.26.7):**
v0.9.0 added `RefreshQuoteAccountRequirements` (this list named 30 of its
claimed 31 — the missing entry was that one). v0.10.0 added
`GetTransferReceipt` + `GetTransferPayoutInfo`. Exported `type` declarations:
91 at v0.9.0 → **92** today (only `TransferPayoutInfo` added); const groups
stable at 22. The "74 types" baseline below was a curated-list count, not the
`go doc -all` measure — use the `go doc -all` numbers for future diffs.

**Webhook growth (same-day pass):** `CreateProfileWebhookSubscription`,
`ListProfileWebhookSubscriptions`, `GetProfileWebhookSubscription`,
`DeleteProfileWebhookSubscription`. Exported types 92 → **107**; const groups
22 → **25** (`WebhookEventType` is an open enum with 33 documented constants).

### Request/response and value types (74 types total)

- Branded IDs (7): `ProfileID`, `BalanceID`, `TransactionID`, `TransferID`, `RecipientID`, `QuoteID` (string/UUID), `BalanceTransactionID` (+ 7 `*Brand` phantom types — brand types are exported by go-branded-id's pattern; consumers never reference them directly).
- Value objects: `Money` (no arithmetic by design), `Currency`, enums (`ProfileType`, `BalanceType`, `InvestmentState`, `TransactionType`, `TransferStatus`, `QuoteStatus`, `QuoteRateType`, `QuoteProvidedAmountType`, `QuoteNoticeType`, `PayIn`, `PayOut`, `DetailType`, `FundingType`, `FundingStatus`, `FundingErrorCode`, `StatementFormat`, `AccountDetailsStatus`, `ReceiveOptionType`).
- Request types (11): `CreateQuoteRequest`, `CreateRecipientRequest`, `CreateTransferRequest`, `ValidateTransferRequirementsRequest`, `QuoteAccountRequirementsRequest`, `ListTransactionsRequest`, `ListTransfersRequest`, `ListRecipientsRequest`, `GetStatementRequest`, `CreateBalanceRequest`, `ValidateTransferRequirementsRequest`-adjacent `TransferRequirementsDetails`.
- Result types (19): `Profile`, `Balance`, `TotalFunds`, `MultiCurrencyAccount`, `BankAccountDetails`, `ReceiveOption(+Description)`, `CallToAction`, `CurrencyInfo`, `Transaction`, `TransactionExchange`, `ListTransactionsResponse`, `Transfer`, `Quote`(+`QuotePaymentOption`, `QuoteFee`, `QuoteNotice`), `Recipient`, `DeliveryEstimate`, `ExchangeRate`, `TransferRequirement`(+`Form`, `Field`, `Value`), `AccountRequirement`, `User`(+`UserDetails`, `UserAddress`), `FundTransferResult`.
- Errors (6): `APIError`, `RateLimitError`, `AuthError`, `SCAChallengeError`, `NotFoundError`, `ServerError`.
- Client/config: `Client`, `Doer`, `Option` (+9 `With*` funcs), `Logger`, `RequestLog`, `RequestLogFunc`, `RequestCorrelationID` ctx helper.
- Helpers: `ParseWebhookPublicKey`, `VerifyWebhookSignature`, `HeaderWebhookSignature`, `HeaderTwoFAApproval(+)Result`, `MissingTransferDetails`, `NewCurrency`, `New*ID` constructors, `ProductionURL`, `SandboxURL`.

## 2. Godoc review findings (passes 1 + 2)

| # | Symbol                                                                           | Finding                                                                                                                   | Severity | Status                                       |
| - | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------- |
| 1 | `ListTransfers`                                                                  | Doc referenced unexported `transferPageSize` name                                                                         | Minor    | FIXED this audit: "pages of 100"             |
| 2 | `CreateUnauthenticatedQuote`                                                     | "illustrative quote" didn't state the purpose                                                                             | Minor    | FIXED this audit: "public rate preview"      |
| 3 | `WithCorrelationID`                                                              | Dangling `[WithRequestCorrelationID]` ref (function didn't exist)                                                         | Medium   | FIXED earlier today: function implemented    |
| 4 | `GetBalance`                                                                     | Legacy doc claimed no direct endpoint existed                                                                             | Medium   | FIXED earlier today: direct endpoint shipped |
| 5 | All funding/statement/webhook symbols                                            | New this session; docs reviewed against the OpenAPI spec                                                                  | —        | Clean                                        |
| 6 | Enum types                                                                       | Open-string enums (`TransferStatus`, `QuoteStatus`, `FundingErrorCode`) correctly document pass-through of unknown values | —        | Clean                                        |
| 7 | `Money`                                                                          | Correctly documents the no-arithmetic contract                                                                            | —        | Clean                                        |
| 8 | `ListBalances`                                                                   | Stale claim "Wise exposes no per-balance endpoint" (false since v0.9.0's direct-endpoint `GetBalance`)                    | Medium   | FIXED in the 2026-09-13 re-audit             |
| 9 | `RefreshQuoteAccountRequirements`, `GetTransferReceipt`, `GetTransferPayoutInfo` | Godoc pass over the 3 post-audit methods: routes, 404/nilability semantics, two-pass flow all spec-accurate               | —        | Clean                                        |

No stale `file:line` references remain in godoc (those live in FEATURES.md, verified separately).

## 3. Breaking-change risk register (what v1.0 freezes)

Ranked by likelihood a future change breaks consumers:

1. **Struct field sets of result types** — ADDING fields is safe; RENAMING/REMOVING breaks struct literals and field access. Highest-usage fields: `Transfer.Status/Source/Target`, `Quote.PaymentOptions`, `Transaction.Amount/Total`. Risk: Wise adding wire fields → we add struct fields (safe). Verdict: freeze names now.
2. **Method signatures** — 31 methods. Any parameter change is breaking. Variadic-option patterns were deliberately avoided in favor of request structs where growth is plausible (`QuoteAccountRequirementsRequest`, `GetStatementRequest`). Methods with bare ID params (`FundTransfer(ctx, ProfileID, TransferID)`, `GetBalance`, `CancelTransfer`) are simple enough to be stable; growth needs would go through new methods.
3. **Error type hierarchy** — `APIError` embedded in all subtypes; consumers use `errors.AsType[*wise.RateLimitError]`. Changing embedding or adding new subtypes is additive-safe; REMOVING a subtype breaks `errors.AsType` calls. Freeze the six.
4. **Branded ID underlying types** — `QuoteID`/`TransactionID` are string-branded; the rest int64. A future flip would break `.Get()` arithmetic. Freeze.
5. **Enum string values** — constants mirror Wise's wire values; can only grow (documented pass-through). Safe by design.
6. **`Doer` interface** — consumed by `WithHTTPClient`. Matches `http.RoundTripper`-adjacent shape; stable.
7. **Behavioral contracts worth documenting as frozen**: cents-based `Money` (no float returns), UTC timestamp normalization (zoneless→UTC in, `Z` out), Corruption-classified mappers, Rejection-classified client-side validation, retry exhaustion surfacing the final attempt's typed error.
8. **Receipt 404 semantics** (2026-09-13) — `GetTransferReceipt` answers `*NotFoundError` both for unknown IDs and for transfers that never reached `outgoing_payment_sent`; 404 means "no receipt (yet)", not a hard failure. Freeze the behavior, not just the signature.
9. **MT103 nilability** (2026-09-13) — `TransferPayoutInfo.MT103` is `*string`, nil for non-SWIFT corridors; consumers must nil-check. Freeze pointer-plus-nil-means-absent.
10. **Two-pass requirements flow** (2026-09-13) — `RefreshQuoteAccountRequirements` shares the `AccountRequirement` result type with the GET and always sends `Accept-Minor-Version: 1`; partial forms are validated client-side. Freeze the shared type and the header behavior.

**Recommendation:** the surface is coherent and completely godoc'd; nothing identified blocks the v1.0 tag. The tag remains gated on the user's explicit approval (plan task 25) — this report is the prerequisite input.

## 4. Deferred decisions (recorded, not blocking)

- `Recipient.Details: map[string]string` vs typed per-corridor structs — open question #3; the audit confirms the map is the only shape consumers depend on today, so v1.0 can freeze the map and add typed accessors later if demanded.
- `Page[T]` — two hand-rolled pagination loops exist (transfers, recipients); guard says wait for a third.

## 5. Coverage of the plan's gate set

Every endpoint added this session shipped: wire type + public type + branded ID where a new ID space appeared (`UserID`, `BalanceTransactionID`) + client method + client-side validation + BDD happy path + error paths + corruption mapper tests (where a mapper can fail) + doc rows + flake fileset entry. `nix flake check` passes.

---

_Audit complete. Inputs: `go doc -all` snapshot 2026-08-21, `docs/reviews/wise-api-openapi.json`, FEATURES.md cross-check. Re-audit 2026-09-13 inputs: `go doc -all` snapshot (go1.26.7), v0.9.0-tag worktree diff (`e508572`), CHANGELOG v0.9.0/v0.10.0 sections, balances.go/quotes.go/transfers.go godoc._
